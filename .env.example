# ── Variables de entorno requeridas ───────────────────────────────────────────
# Copia este archivo como .env para pruebas locales (nunca subas .env al repo).
# En GitHub Actions configura estos valores como Repository Secrets.

# Dirección de correo del remitente
SENDER_EMAIL=jmoya@colegiocabodehornos.cl

# Nombre que aparecerá como remitente
SENDER_NAME=Colegio Cabo de Hornos

# Servidor SMTP (Gmail por defecto)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Usuario SMTP (generalmente el mismo que SENDER_EMAIL)
SMTP_USER=jmoya@colegiocabodehornos.cl

# Contraseña de aplicación SMTP
# Para Gmail: genera una en Cuenta de Google → Seguridad → Contraseñas de aplicación
# Para cuentas de G Suite / Google Workspace corporativas el flujo es el mismo.
SMTP_PASS=tu_contrasena_de_aplicacion
